# Changesets

This folder tracks release notes and versioning metadata for Hawkshaw.

Use `pnpm changeset` when you want to record a user-facing or developer-facing change that should be
captured in release history.

