# Scaffold Conventions

- Keep route code in `app/` and non-route code in `src/`.
- Use route groups for area boundaries, not URL changes.
- Keep server integrations behind shared helpers instead of creating ad hoc clients in pages.
- Prefer placeholder types and service boundaries over premature feature logic.
- Treat docs updates as part of implementation, not follow-up cleanup.

