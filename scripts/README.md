# Scripts

This folder is the starting point for Hawkshaw operational scripts.

Current script categories:

- `setup/`: local environment checks and bootstrap helpers

Add product-specific scripts here only when they are reusable project workflow tools, not one-off
throwaway commands.

