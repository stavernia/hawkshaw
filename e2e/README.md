# E2E Placeholder

Mad Brackets carries an end-to-end testing directory in the root starter shape.

Hawkshaw does not need Playwright coverage yet, but this directory is reserved so the next pass can
add host/player flow coverage without reorganizing the repo.

